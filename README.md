# Terraform AWS Web Server

This project deploys a basic Nginx web server on an Ubuntu EC2 instance in AWS using Infrastructure as Code (Terraform).

## Prerequisites
* Terraform installed
* AWS CLI configured

## Usage
1. `terraform init`
2. `terraform plan`
3. `terraform apply`
